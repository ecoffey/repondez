<%= form_for @invite, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :address, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_area :address, :class => 'text_area' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :passphrase, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :passphrase, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :tier, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :tier, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                invites_path, :class => 'btn' %>
  </div>
<% end %>
